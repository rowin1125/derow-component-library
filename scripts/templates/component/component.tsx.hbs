import React from 'react';
import styled from 'styled-components';
import cn from 'classnames';

export interface I{{ pascalCase componentName }} extends React.HtmlHTMLAttributes<HTMLElement> {
  children?: React.ReactChild;
}

const Styled{{ pascalCase componentName }}: any = styled.div``;

const {{ pascalCase componentName }}: React.FC<I{{ pascalCase componentName }}> = ({ children, ...rest }) => (
  <Styled{{ pascalCase componentName }} className={cn('{{ pascalCase componentName}}', {
    'not-rendered-{{ dashCase componentName}}': false,
    'is-rendered-{{ dashCase componentName}}': true,
    })}
    data-testid='{{ dashCase componentName}}-component'
    {...rest}
    >
    {children}
  </Styled{{ pascalCase componentName }}>
)

export default {{ pascalCase componentName }}
